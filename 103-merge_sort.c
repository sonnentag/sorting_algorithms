#include "sort.h"
/**
* merge_sort - function
* @array: array to be sorted
* @size: size of array to sort
*
* Description: function to sort array using top-down merge-sort algo
* Return: Nothing
*/
void merge_sort(int *array, size_t size)
{
	/*int *left_array, *right_array, *final_array;*/
	/*unsigned int i, j;*/

	if (size < 2 || array == NULL)
		return;

	printf("Merging...\n");
	/*printf("[left]: %d\n", left_array);*/
	/*printf("[right]: %d\n", right_array);*/
	printf(", \n");
	/*printf("[Done]: %d %d\n" final_array);*/
}
